import { superAdminMenu } from "./superadmin";
import { cadeteMenu } from "./cadete";
import { administradorClubMenu } from "./administrador_club";

export const ProfileMenu = {
    superAdmin: superAdminMenu,
    administradorClub: administradorClubMenu,
    cadete: cadeteMenu,
}